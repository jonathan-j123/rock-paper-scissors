function computerPlay() {
    return Math.floor(Math.random() * 3 + 1);
}

function gameExecution(computerSelection, userSelection) {
    computerSelection = computerPlay();
    let userInput = prompt("['ROCK', 'PAPER', 'SCISSOR'] - Choose one: ");
    return
}


